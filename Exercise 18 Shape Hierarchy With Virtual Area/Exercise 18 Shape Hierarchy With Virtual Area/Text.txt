Practice Problem: Create a base class Shape with a method area() that returns 0.0. 
Derive two classes, Square (with private side member) and Triangle (with private base and height members). 
Both derived classes must override area() to perform the correct area calculation. 
For Square, return side *side For Triangle, return (base * side)/2.